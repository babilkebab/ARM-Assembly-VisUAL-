		;RICERCA	DI UN ELEMENTO IN UN ARRAY
		
ARRAY		DCD		15,7,-3,7,4,7
NUMEL		DCD		6
TROVATO		FILL		4
NUM_CERC	DCD		42
		
		MOV		R0, #ARRAY         ; R0 TIENE INDIRIZZO ELEMENTO
		LDR		R4, [R0]			; R4 TIENE VALORE ELEMENTO
		MOV		R1, #NUM_CERC		; R1 TIENE VALORE DA CERCARE
		LDR		R1, [R1]
		MOV		R2, #0			; R2 DICE SE E' STATO TROVATO OPPURE NO. 1 SE LO TROVA, 0 SE NON LO TROVA.
		MOV		R3, #NUMEL			; R3 TIENE NUMERO DI ELEMENTI ANCORA DA CERCARE
		LDR		R3, [R3]
		
CONF		CMP		R4, R1
		BEQ		TROV
		
		
CICLO		ADD		R0, R0, #4
		LDR		R4, [R0]
		SUB		R3, R3, #1
		CMP		R3, #0
		BGT		CONF
		BEQ		FINE
		
TROV		ADD		R2, R2, #1
		
		
FINE		MOV		R5, #TROVATO
		STR		R2, [R5]