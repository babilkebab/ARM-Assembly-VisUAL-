		;OCCORRENZE	DI UN ELEMENTO IN UN ARRAY
		
ARRAY		DCD		15,7,-3,7,4,7
NUMEL		DCD		6
OCCOR		FILL		4
NUM_CERC	DCD		7
		
		MOV		R0, #ARRAY         ; R0 TIENE INDIRIZZO ELEMENTO
		LDR		R4, [R0]			; R4 TIENE VALORE ELEMENTO
		MOV		R1, #NUM_CERC		; R1 TIENE VALORE DA CERCARE
		LDR		R1, [R1]
		MOV		R2, #0			; R2 CONTA OCCCORRENZE
		MOV		R3, #NUMEL			; R3 TIENE NUMERO DI ELEMENTI ANCORA DA CERCARE
		LDR		R3, [R3]
		
CONF		CMP		R4, R1
		BEQ		AGG
		
		
CICLO		ADD		R0, R0, #4
		LDR		R4, [R0]
		SUB		R3, R3, #1
		CMP		R3, #0
		BGT		CONF
		BEQ		FINE
		
		
		
AGG		ADD		R2, R2, #1
		ADD		R0, R0, #4
		LDR		R4, [R0]
		SUB		R3, R3, #1
		CMP		R3, #0
		BGT		CONF
		BEQ		FINE
		
FINE		MOV		R5, #OCCOR
		STR		R2, [R5]